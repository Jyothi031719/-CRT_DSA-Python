def deleteIndex(self,Index):
    if self.head
       return -1
    if index ==0:
        deleyeAtstart():
        return
    if index==self.size-1:
        deleteFromEnd()
        return
    curr=self.head 
    for i in rage(index-1):
        curr=curr.next
    temp=curr.next
    curr.next = temp.next
    temp.next=None
    return temp